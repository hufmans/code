# 99行实现cpp线程池

完整代码：

```cpp
#include <iostream>
#include <thread>
#include <vector>
#include <queue>
#include <functional>
#include <mutex>
#include <condition_variable>
#include <future>
#include <optional>

class ThreadPool {
public:
    ThreadPool(size_t numThreads);
    ~ThreadPool();

    template<class F, class... Args>
    auto enqueue(F&& f, Args&&... args)
        -> std::future<typename std::invoke_result<F, Args...>::type>;

private:
    std::vector<std::thread> workers;
    std::queue<std::function<void()>> tasks;

    std::mutex queueMutex;
    std::condition_variable condition;
    bool stop = false;
};

ThreadPool::ThreadPool(size_t numThreads) {
    for (size_t i = 0; i < numThreads; ++i) {
        workers.emplace_back([this] {
            while (true) {
                std::function<void()> task;

                {
                    std::unique_lock<std::mutex> lock(this->queueMutex);
                    this->condition.wait(lock, [this] { return this->stop || !this->tasks.empty(); });
                    if (this->stop && this->tasks.empty())
                        return;
                    task = std::move(this->tasks.front());
                    this->tasks.pop();
                }

                task();
            }
        });
    }
}

ThreadPool::~ThreadPool() {
    {
        std::unique_lock<std::mutex> lock(queueMutex);
        stop = true;
    }
    condition.notify_all();
    for (std::thread &worker : workers) {
        worker.join();
    }
}

template<class F, class... Args>
auto ThreadPool::enqueue(F&& f, Args&&... args)
    -> std::future<typename std::invoke_result<F, Args...>::type> {
    using return_type = typename std::invoke_result<F, Args...>::type;

    auto task = std::make_shared<std::packaged_task<return_type()>>(
        std::bind(std::forward<F>(f), std::forward<Args>(args)...)
    );

    std::future<return_type> res = task->get_future();
    {
        std::unique_lock<std::mutex> lock(queueMutex);
        if (stop) {
            throw std::runtime_error("enqueue on stopped ThreadPool");
        }
        tasks.emplace([task]() { (*task)(); });
    }
    condition.notify_one();
    return res;
}

int main() {
    ThreadPool pool(4); // 创建一个包含 4 个线程的线程池

    std::vector<std::future<int>> results;

    for (int i = 0; i < 10; ++i) {
        results.emplace_back(pool.enqueue([i] {
            std::this_thread::sleep_for(std::chrono::milliseconds(100));
            return i * i;
        }));
    }

    for (auto&& result : results) {
        std::cout << result.get() << ' ';
    }

    return 0;
}

```



### 1. 类定义

```
cpp复制代码class ThreadPool {
public:
    ThreadPool(size_t numThreads);
    ~ThreadPool();
    template<class F, class... Args>
    auto enqueue(F&& f, Args&&... args) -> std::future<typename std::invoke_result<F, Args...>::type>;

private:
    std::vector<std::thread> workers;
    std::queue<std::function<void()>> tasks;
    std::mutex queueMutex;
    std::condition_variable condition;
    bool stop = false;
};
```

- **构造函数**和**析构函数**：负责初始化和清理线程池。

- **enqueue**：用于将任务加入任务队列并返回一个 `future`，可以用来获取结果。

- 私有成员

  ：

  - `workers`：存储工作线程。
  - `tasks`：存储待执行的任务。
  - `queueMutex`：保护任务队列的互斥量。
  - `condition`：用于线程间同步的条件变量。
  - `stop`：标志线程池是否正在停止。

### 2. 构造函数

```
cpp复制代码ThreadPool::ThreadPool(size_t numThreads) {
    for (size_t i = 0; i < numThreads; ++i) {
        workers.emplace_back([this] {
            while (true) {
                std::function<void()> task;

                {
                    std::unique_lock<std::mutex> lock(this->queueMutex);
                    this->condition.wait(lock, [this] { return this->stop || !this->tasks.empty(); });
                    if (this->stop && this->tasks.empty())
                        return;
                    task = std::move(this->tasks.front());
                    this->tasks.pop();
                }

                task();
            }
        });
    }
}
```

- 在构造函数中，我们创建多个工作线程。
- 每个线程执行一个无限循环，等待任务。
- `condition.wait` 用于使线程在任务队列为空时阻塞，直到有任务或线程池停止。

### 3. `enqueue` 方法

```
cpp复制代码template<class F, class... Args>
auto ThreadPool::enqueue(F&& f, Args&&... args)
    -> std::future<typename std::invoke_result<F, Args...>::type> {
    using return_type = typename std::invoke_result<F, Args...>::type;

    auto task = std::make_shared<std::packaged_task<return_type()>>(
        std::bind(std::forward<F>(f), std::forward<Args>(args)...)
    );

    std::future<return_type> res = task->get_future();
    {
        std::unique_lock<std::mutex> lock(queueMutex);
        if (stop) {
            throw std::runtime_error("enqueue on stopped ThreadPool");
        }
        tasks.emplace([task]() { (*task)(); });
    }
    condition.notify_one();
    return res;
}
```

- **模板参数**：支持任何可调用对象（函数、Lambda 等）。
- **`packaged_task`**：将任务封装起来，使其可以异步执行并返回结果。
- **返回值**：`std::future` 允许调用者在未来某个时刻获取结果。
- **互斥锁**：在对任务队列进行操作时，确保线程安全。

### 4. 析构函数

```
cpp复制代码ThreadPool::~ThreadPool() {
    {
        std::unique_lock<std::mutex> lock(queueMutex);
        stop = true;
    }
    condition.notify_all();
    for (std::thread &worker : workers) {
        worker.join();
    }
}
```

- 设置 `stop` 为 `true`，通知所有线程停止。
- 使用 `notify_all` 唤醒所有被阻塞的线程，以便它们可以检查 `stop` 标志并退出。
- 使用 `join` 等待每个线程完成。

### 5. 主函数示例

```
cpp复制代码int main() {
    ThreadPool pool(4); // 创建一个包含 4 个线程的线程池
    std::vector<std::future<int>> results;

    for (int i = 0; i < 10; ++i) {
        results.emplace_back(pool.enqueue([i] {
            std::this_thread::sleep_for(std::chrono::milliseconds(100));
            return i * i;
        }));
    }

    for (auto&& result : results) {
        std::cout << result.get() << ' ';
    }

    return 0;
}
```

- 创建一个包含 4 个线程的线程池。
- 向线程池添加 10 个任务，计算每个数字的平方。
- 使用 `future.get()` 获取每个任务的结果并打印。



# std::thread

`std::thread` 是 C++11 引入的一个类，用于表示和管理线程。下面是 `std::thread` 数据结构的一些关键特点和成员：

### 1. 主要功能

- **创建线程**：通过传递可调用对象（函数、Lambda、成员函数等）来创建新的线程。
- **管理线程生命周期**：提供方法来启动、停止和等待线程的完成。
- **线程 ID**：每个线程都有一个唯一的线程标识符，可以用于识别和管理线程。

### 2. 主要成员函数

- **构造函数**：`std::thread` 的构造函数接受可调用对象及其参数，启动线程。

  ```
  cpp
  
  
  复制代码
  std::thread t(func, arg1, arg2); // 创建线程并运行 func(arg1, arg2)
  ```

- **join()**：等待线程完成。

  ```
  cpp
  
  
  复制代码
  t.join(); // 主线程等待 t 完成
  ```

- **detach()**：将线程分离，允许它在后台运行，主线程不再管理。

  ```
  cpp
  
  
  复制代码
  t.detach(); // 分离线程，允许独立运行
  ```

- **joinable()**：检查线程是否可以被连接（即是否仍然可用）。

  ```
  cpp
  
  
  复制代码
  if (t.joinable()) { ... }
  ```

- **get_id()**：获取线程的唯一标识符。

  ```
  cpp
  
  
  复制代码
  std::thread::id id = t.get_id();
  ```

- **swap()**：交换两个线程对象。

  ```
  cpp复制代码std::thread t2;
  t.swap(t2); // 交换 t 和 t2
  ```

### 3. 线程 ID

- **`std::thread::id`**：表示线程的唯一标识符，可以与其他 `std::thread::id` 对象进行比较。

### 4. 特性

- **非拷贝**：`std::thread` 对象不可复制（只能移动），这意味着一旦线程开始运行，它的状态不能被复制。
- **RAII 语义**：在 `std::thread` 被销毁时，如果线程仍然在运行而没有被连接，会导致程序终止。因此，必须显式调用 `join()` 或 `detach()`。

### 示例代码

以下是一个简单的示例，展示如何使用 `std::thread`：

```
cpp复制代码#include <iostream>
#include <thread>

void printHello(int id) {
    std::cout << "Hello from thread " << id << std::endl;
}

int main() {
    std::thread t1(printHello, 1);
    std::thread t2(printHello, 2);

    t1.join(); // 等待 t1 完成
    t2.join(); // 等待 t2 完成

    return 0;
}
```

### 总结

`std::thread` 提供了一个简单而强大的接口，用于在 C++ 中创建和管理线程。通过使用成员函数和线程 ID，可以方便地控制线程的执行和生命周期。

# std::future

`std::future` 是 C++11 引入的一个类，用于表示异步操作的结果。它提供了一种机制，可以在将来的某个时间点获取一个异步任务的返回值或异常。以下是 `std::future` 的一些关键特点和用法：

### 主要功能

1. **异步结果**：
   - `std::future` 可以存储异步操作的结果，当结果可用时，可以通过它访问。
2. **状态检查**：
   - 可以检查异步操作的状态，比如是否已经完成、是否有异常等。
3. **阻塞获取**：
   - 使用 `get()` 方法可以阻塞当前线程，直到异步操作完成，并获取结果。

### 主要成员函数

- **`get()`**：

  - 返回异步操作的结果。如果操作未完成，该方法会阻塞直到结果可用。
  - 如果异步操作抛出异常，调用 `get()` 会重新抛出该异常。

  ```
  cpp复制代码std::future<int> fut = std::async(std::launch::async, [] { return 42; });
  int result = fut.get(); // 阻塞直到结果可用
  ```

- **`valid()`**：

  - 检查 `std::future` 对象是否有效（即是否与任何异步操作关联）。

  ```
  cpp
  
  
  复制代码
  if (fut.valid()) { ... }
  ```

- **`wait()`**：

  - 阻塞当前线程，直到异步操作完成，但不返回结果。

  ```
  cpp
  
  
  复制代码
  fut.wait(); // 等待异步操作完成
  ```

- **`wait_for()`** 和 **`wait_until()`**：

  - 允许设置超时时间，等待异步操作完成。

  ```
  cpp
  
  
  复制代码
  auto status = fut.wait_for(std::chrono::seconds(1)); // 等待1秒
  ```

### 示例代码

以下是一个简单示例，展示如何使用 `std::future`：

```
cpp复制代码#include <iostream>
#include <future>
#include <chrono>

int asyncTask() {
    std::this_thread::sleep_for(std::chrono::seconds(2));
    return 42;
}

int main() {
    std::future<int> fut = std::async(std::launch::async, asyncTask);

    // 在这里可以做其他事情

    int result = fut.get(); // 阻塞，直到任务完成
    std::cout << "Result: " << result << std::endl;

    return 0;
}
```

### 总结

`std::future` 提供了强大的异步编程支持，使得处理多线程操作变得更加简单和安全。通过它，可以轻松地管理异步任务的结果和状态，避免回调地狱，并提高程序的可读性。

# std::function

`<functional>` 是 C++ 标准库中的一个头文件，提供了功能强大的工具和模板，用于函数对象、函数指针、绑定函数和其他与函数相关的功能。以下是一些主要内容和用途：

### 主要功能

1. **函数对象**：

   - `std::function` 是一个可调用对象的泛型容器，可以存储任何可调用的目标（如函数、Lambda 表达式、绑定表达式或其他函数对象）。

   ```
   cpp
   
   
   复制代码
   std::function<int(int, int)> add = [](int a, int b) { return a + b; };
   ```

2. **绑定函数**：

   - `std::bind` 用于创建一个新的可调用对象，将某些参数绑定到函数或可调用对象。

   ```
   cpp复制代码auto add5 = std::bind(add, 5, std::placeholders::_1);
   int result = add5(10); // 结果为 15
   ```

3. **函数适配器**：

   - `std::ref` 和 `std::cref` 用于在传递对象时避免复制，允许以引用方式传递。

   ```
   cpp复制代码int x = 10;
   std::function<void()> func = std::bind(print, std::ref(x));
   ```

4. **可调用适配器**：

   - `std::mem_fn` 可以用于创建指向成员函数的可调用对象。

   ```
   cpp复制代码struct MyStruct {
       void display() { std::cout << "Hello!" << std::endl; }
   };
   MyStruct obj;
   auto func = std::mem_fn(&MyStruct::display);
   func(obj); // 调用 obj.display()
   ```

### 常用模板

- **`std::function`**：用于存储可调用对象的类型安全包装。
- **`std::bind`**：用于绑定函数参数。
- **`std::ref` 和 `std::cref`**：用于传递引用。
- **`std::mem_fn`**：用于指向类成员函数。

### 示例代码

以下是一个示例，展示了如何使用 `<functional>` 中的一些功能：

```
cpp复制代码#include <iostream>
#include <functional>

void print(int x) {
    std::cout << x << std::endl;
}

int main() {
    // 使用 std::function
    std::function<void(int)> func = print;
    func(5); // 输出 5

    // 使用 std::bind
    auto bindFunc = std::bind(print, 10);
    bindFunc(); // 输出 10

    // 使用 std::ref
    int value = 20;
    std::function<void()> refFunc = std::bind(print, std::ref(value));
    refFunc(); // 输出 20

    return 0;
}
```

### 总结

`<functional>` 提供了强大的工具来处理可调用对象，使得函数的使用更加灵活和简洁。通过使用这些工具，可以轻松地进行函数绑定、适配和引用传递，从而提高代码的可读性和可维护性。

# chrono

`<chrono>` 是 C++ 标准库中的一个头文件，提供了时间和日期的处理功能，包括计时、时钟、时间段等。以下是一些主要内容和用途：

### 主要功能

1. **时钟**：

   - 提供不同类型的时钟，例如：
     - `std::chrono::system_clock`：用于获取系统当前时间，通常与现实世界时间对应。
     - `std::chrono::steady_clock`：用于获取不随系统时间调整而改变的时间，适合用于计时。
     - `std::chrono::high_resolution_clock`：提供最高分辨率的时钟，通常基于 `steady_clock`。

2. **时间段**：

   - 使用 `std::chrono::duration` 表示时间段，可以通过不同单位（如秒、毫秒、微秒等）进行表示。

   - 例如：

     ```
     cpp复制代码std::chrono::duration<int> seconds(5); // 5秒
     std::chrono::duration<double, std::milli> milliseconds(1500); // 1500毫秒
     ```

3. **时间点**：

   - 使用 `std::chrono::time_point` 表示特定的时间点，可以与时钟结合使用。

   - 例如：

     ```
     cpp
     
     
     复制代码
     auto now = std::chrono::system_clock::now(); // 当前时间点
     ```

4. **时间计算**：

   - 可以进行时间段的加法和减法，计算时间差等操作。

   - 例如：

     ```
     cpp复制代码auto start = std::chrono::steady_clock::now();
     // 执行一些操作
     auto end = std::chrono::steady_clock::now();
     auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(end - start);
     ```

5. **睡眠**：

   - 使用 `std::this_thread::sleep_for` 和 `std::this_thread::sleep_until` 来使线程暂停。

   - 例如：

     ```
     cpp
     
     
     复制代码
     std::this_thread::sleep_for(std::chrono::seconds(1)); // 暂停1秒
     ```

### 示例代码

以下是一个简单的示例，展示如何使用 `<chrono>`：

```
cpp复制代码#include <iostream>
#include <chrono>
#include <thread>

int main() {
    auto start = std::chrono::high_resolution_clock::now();

    // 模拟一些处理
    std::this_thread::sleep_for(std::chrono::seconds(2));

    auto end = std::chrono::high_resolution_clock::now();
    std::chrono::duration<double> elapsed = end - start;

    std::cout << "Elapsed time: " << elapsed.count() << " seconds" << std::endl;

    return 0;
}
```

### 总结

`<chrono>` 提供了强大的时间和日期处理功能，使得在 C++ 中进行计时、延时和时间计算变得简单而灵活。通过使用不同的时钟和时间段，可以轻松实现高效的时间管理。

# std::condition_variable

`std::condition_variable` 是 C++ 标准库中的一个类，用于线程间的同步。它允许线程在某个条件不满足时进入等待状态，并在条件满足时被其他线程唤醒。下面是对 `std::condition_variable` 的详细介绍。

### 主要功能

1. **条件等待**：
   - 线程可以通过 `wait` 方法进入等待状态，直到被通知条件已经满足。
2. **通知机制**：
   - 通过 `notify_one` 或 `notify_all` 方法，其他线程可以通知一个或所有等待的线程，表明条件已经改变。

### 主要成员函数

- **`wait`**：

  - 用于使当前线程等待，并在条件变量上释放锁。
  - 一旦被通知，线程会重新获取锁并继续执行。

  ```
  cpp复制代码std::unique_lock<std::mutex> lock(mutex);
  condition_variable.wait(lock, [] { return condition; });
  ```

- **`notify_one`**：

  - 唤醒一个在该条件变量上等待的线程。

  ```
  cpp
  
  
  复制代码
  condition_variable.notify_one();
  ```

- **`notify_all`**：

  - 唤醒所有在该条件变量上等待的线程。

  ```
  cpp
  
  
  复制代码
  condition_variable.notify_all();
  ```

### 使用示例

以下是一个简单的生产者-消费者示例，展示了如何使用 `std::condition_variable`：

```
cpp复制代码#include <iostream>
#include <queue>
#include <thread>
#include <condition_variable>

std::queue<int> buffer;
const unsigned int maxSize = 10;
std::mutex mtx;
std::condition_variable cv;

void producer() {
    for (int i = 0; i < 20; ++i) {
        std::unique_lock<std::mutex> lock(mtx);
        cv.wait(lock, [] { return buffer.size() < maxSize; }); // 等待直到缓冲区有空间
        buffer.push(i);
        std::cout << "Produced: " << i << std::endl;
        lock.unlock();
        cv.notify_all(); // 通知消费者
    }
}

void consumer() {
    for (int i = 0; i < 20; ++i) {
        std::unique_lock<std::mutex> lock(mtx);
        cv.wait(lock, [] { return !buffer.empty(); }); // 等待直到缓冲区不为空
        int value = buffer.front();
        buffer.pop();
        std::cout << "Consumed: " << value << std::endl;
        lock.unlock();
        cv.notify_all(); // 通知生产者
    }
}

int main() {
    std::thread prod(producer);
    std::thread cons(consumer);

    prod.join();
    cons.join();

    return 0;
}
```

### 代码解析

1. **生产者**：
   - 生产者线程会检查缓冲区是否已满，如果已满则等待。
   - 生产一个新项目后，唤醒所有等待的消费者。
2. **消费者**：
   - 消费者线程会检查缓冲区是否为空，如果为空则等待。
   - 消费一个项目后，唤醒所有等待的生产者。

### 总结

`std::condition_variable` 是用于实现线程间同步的重要工具，能够有效地管理线程的等待和通知机制，特别是在生产者-消费者模式中。

# std::unique_lock

`std::unique_lock` 是 C++ 标准库中的一个类，用于管理互斥锁（`std::mutex`）的锁定和解锁。它提供了比 `std::lock_guard` 更灵活的锁定机制，适用于需要在不同作用域中锁定和解锁的情况。以下是对 `std::unique_lock` 的详细介绍。

### 主要功能

1. **RAII 风格**：
   - `std::unique_lock` 是基于 RAII（资源获取即初始化）原则的，构造时自动锁定互斥锁，析构时自动解锁。这确保了在作用域结束时互斥锁总是被解锁，避免了死锁。
2. **灵活性**：
   - 相比 `std::lock_guard`，`std::unique_lock` 允许手动解锁和重新锁定，使得在复杂逻辑中更灵活。
3. **条件变量与互斥锁结合使用**：
   - `std::unique_lock` 与 `std::condition_variable` 结合使用时非常方便，因为它可以在等待时自动解锁互斥锁。

### 主要成员函数

- **构造函数**：
  - `std::unique_lock<std::mutex> lock(mutex);`：构造时锁定指定的互斥锁。
- **解锁**：
  - `lock.unlock();`：手动解锁互斥锁。
- **重新锁定**：
  - `lock.lock();`：重新锁定互斥锁。
- **是否持有锁**：
  - `lock.owns_lock();`：检查 `std::unique_lock` 是否持有互斥锁。

### 示例代码

以下是一个简单的示例，展示如何使用 `std::unique_lock`：

```
cpp复制代码#include <iostream>
#include <thread>
#include <mutex>
#include <chrono>

std::mutex mtx;

void printId(int id) {
    std::unique_lock<std::mutex> lock(mtx); // 锁定互斥锁
    std::cout << "Thread " << id << " is running\n";
    std::this_thread::sleep_for(std::chrono::milliseconds(100)); // 模拟工作
    // lock 在这里超出作用域后会自动解锁
}

int main() {
    std::thread threads[10];
    for (int i = 0; i < 10; ++i) {
        threads[i] = std::thread(printId, i);
    }
    for (auto& th : threads) {
        th.join();
    }
    return 0;
}
```

### 代码解析

1. **锁定互斥锁**：
   - 在 `printId` 函数中，`std::unique_lock<std::mutex> lock(mtx);` 创建一个 `unique_lock` 对象并锁定互斥锁 `mtx`。
2. **输出线程 ID**：
   - 在持有锁的情况下，输出线程的 ID。
3. **自动解锁**：
   - 当 `lock` 超出作用域时，互斥锁 `mtx` 会自动解锁，确保在函数返回时锁被释放。

### 总结

`std::unique_lock` 提供了一种灵活和安全的方式来管理互斥锁，适用于需要在复杂场景中进行锁定和解锁的情况。它与条件变量结合使用时尤其强大，有效避免了可能的死锁问题。

# 尾返回类型

```cpp
template<class F, class... Args> 
auto ThreadPool::enqueue(F&& f, Args&&... args)    -> std::future<typename std::invoke_result<F, Args...>::type>
```

尾返回类型（Trailing Return Type）是 C++11 引入的一种语法，用于在函数声明中将返回类型放在参数列表之后，使用 `->` 语法。尾返回类型主要用于以下情况：

1. **简化复杂类型**：当返回类型较复杂，特别是涉及模板时，使用尾返回类型可以提高可读性。
2. **与 `auto` 结合使用**：可以使用 `auto` 作为返回类型，并在后面指定具体的返回类型。
3. **支持类型推断**：可以更好地与 `decltype` 等类型推导机制结合使用。

### 语法示例

基本的尾返回类型语法如下：

```cpp
cpp复制代码auto functionName(parameters) -> returnType {
    // function body
}
```



# std::invoke_result

`std::invoke_result` 是 C++17 引入的一个类型特征，用于推导给定可调用对象（如函数、Lambda、函数对象等）在特定参数下的返回类型。它是通过模板元编程提供的，能够在编译时确定函数的返回类型，而不需要实际调用它。

### 主要功能

1. **类型推导**：
   - `std::invoke_result` 可以根据可调用对象及其参数类型，推导出该调用的返回类型。
2. **更好的泛型编程**：
   - 它使得模板函数能够更容易地处理不同类型的可调用对象，同时能够确保返回类型的正确性。

### 语法

```
cpp复制代码template<typename F, typename... Args>
struct std::invoke_result;
```

- `F` 是可调用对象的类型。
- `Args...` 是可调用对象所需的参数类型。

### 使用示例

1. **简单用法**：

```
cpp复制代码#include <iostream>
#include <type_traits>

int add(int a, int b) {
    return a + b;
}

int main() {
    using result_type = std::invoke_result<decltype(add), int, int>::type;
    std::cout << "Result type of add: " << typeid(result_type).name() << std::endl; // 输出: int
    return 0;
}
```

在这个例子中，`std::invoke_result` 用于获取 `add` 函数在接受两个 `int` 参数时的返回类型。

1. **与模板结合使用**：

```
cpp复制代码#include <iostream>
#include <type_traits>

template<typename T>
auto getValue(T&& obj) -> std::invoke_result_t<decltype(&T::value), T> {
    return obj.value; // 假设 obj 有一个成员 value
}
```

这里，`std::invoke_result_t` 是 `std::invoke_result` 的别名，用于直接获取类型，而不需要使用 `::type`。

1. **与 `std::future` 结合**：

在你之前提到的线程池 `enqueue` 函数中，`std::invoke_result` 被用来推导提交任务的返回类型：

```
cpp复制代码template<class F, class... Args>
auto ThreadPool::enqueue(F&& f, Args&&... args)
    -> std::future<typename std::invoke_result<F, Args...>::type> {
    // 函数实现
}
```

### 总结

`std::invoke_result` 提供了一种强大而灵活的方式来推导可调用对象的返回类型，使得在泛型编程中能够更好地处理各种类型的可调用对象。

# std::invoke_result和decltype区别

`std::invoke_result` 和 `decltype` 都是 C++ 中用于类型推导的工具，但它们的用途和功能有所不同。以下是它们的主要区别：

### 1. **功能**

- **`std::invoke_result`**：

  - 用于推导给定可调用对象（如函数、Lambda、函数对象等）在特定参数类型下的返回类型。
  - 它主要用于模板编程，能够在编译时确定函数调用的返回类型，而不需要实际调用函数。

  ```
  cpp
  
  
  复制代码
  std::invoke_result<F, Args...>::type
  ```

- **`decltype`**：

  - 用于获取表达式的类型。它可以用于获取任何表达式（包括变量、函数调用、成员访问等）的类型。
  - 适用于更广泛的场景，除了可调用对象的返回类型外，还可以获取任何类型。

  ```
  cpp
  
  
  复制代码
  decltype(expression)
  ```

### 2. **使用场景**

- **`std::invoke_result`**：

  - 主要用于获取可调用对象的返回类型，特别是在模板中需要动态处理不同类型的可调用对象时。

  ```
  cpp
  
  
  复制代码
  using result_type = std::invoke_result<decltype(func), int, double>::type;
  ```

- **`decltype`**：

  - 可用于获取任何表达式的类型。例如，你可以用它获取变量类型、函数返回类型、数组元素类型等。

  ```
  cpp复制代码int x = 42;
  decltype(x) y; // y 的类型是 int
  ```

### 3. **语法和表达方式**

- `std::invoke_result`

  ：

  - 需要两个参数：可调用对象的类型和参数类型。

- `decltype`

  ：

  - 只需要一个表达式，可以是任意有效的 C++ 表达式。

### 示例比较

```
cpp复制代码#include <iostream>
#include <type_traits>

// 示例函数
int add(int a, int b) {
    return a + b;
}

int main() {
    // 使用 std::invoke_result
    using invoke_result_type = std::invoke_result<decltype(add), int, int>::type;
    std::cout << "invoke_result type: " << typeid(invoke_result_type).name() << std::endl; // 输出: int

    // 使用 decltype
    decltype(add(1, 2)) decltype_result; // decltype_result 的类型为 int
    std::cout << "decltype type: " << typeid(decltype_result).name() << std::endl; // 输出: int

    return 0;
}
```

### 总结

- **`std::invoke_result`** 专注于推导可调用对象的返回类型，适用于模板编程。
- **`decltype`** 用于获取任意表达式的类型，功能更广泛。



# 代码解析

这段代码用于创建一个共享指针，指向一个打包的任务（`std::packaged_task`），并将一个可调用对象（函数或 Lambda）绑定到这个任务上。让我们逐步解析这行代码的各个部分。

```
cpp复制代码auto task = std::make_shared<std::packaged_task<return_type()>>(
    std::bind(std::forward<F>(f), std::forward<Args>(args)...)
);
```

1. **`std::make_shared`**：
   - `std::make_shared` 用于创建一个 `std::shared_ptr`，它管理动态分配的对象。这里，它创建了一个 `std::packaged_task` 的实例。
2. **`std::packaged_task<return_type()>`**：
   - `std::packaged_task` 是一个模板类，用于封装任何可调用对象（函数、Lambda 等），并将其结果存储在 `std::future` 中。`return_type()` 表示该任务没有参数并返回 `return_type` 类型的值。
3. **`std::bind`**：
   - `std::bind` 用于创建一个可调用对象，将参数 `f`（可以是函数或 Lambda）与其参数 `args...` 绑定在一起。这样，当 `packaged_task` 被执行时，它会调用 `f`，并使用 `args...` 作为参数。
4. **`std::forward<F>(f)` 和 `std::forward<Args>(args)...`**：
   - `std::forward` 用于完美转发参数，以保持其值类别（左值或右值）。这样可以确保在传递给 `std::bind` 时，参数的类型保持不变。





# 参考资料

## 第 7 章 并行与并发



## 7.1 并行基础

`std::thread` 用于创建一个执行的线程实例，所以它是一切并发编程的基础，使用时需要包含 `<thread>` 头文件， 它提供了很多基本的线程操作，例如 `get_id()` 来获取所创建线程的线程 ID，使用 `join()` 来等待一个线程结束（与该线程汇合）等等，例如：

```
#include <iostream>
#include <thread>

int main() {
    std::thread t([](){
        std::cout << "hello world." << std::endl;
    });
    t.join();
    return 0;
}
```



## 7.2 互斥量与临界区

我们在操作系统、亦或是数据库的相关知识中已经了解过了有关并发技术的基本知识，`mutex` 就是其中的核心之一。 C++11 引入了 `mutex` 相关的类，其所有相关的函数都放在 `<mutex>` 头文件中。

`std::mutex` 是 C++11 中最基本的互斥量类，可以通过构造 `std::mutex` 对象创建互斥量， 而通过其成员函数 `lock()` 可以进行上锁，`unlock()` 可以进行解锁。 但是在实际编写代码的过程中，最好不去直接调用成员函数， 因为调用成员函数就需要在每个临界区的出口处调用 `unlock()`，当然，还包括异常。 而 C++11 为互斥量提供了一个 RAII 机制的模板类 `std::lock_guard`。

RAII 在不失代码简洁性的同时，很好的保证了代码的异常安全性。

在 RAII 用法下，对于临界区的互斥量的创建只需要在作用域的开始部分，例如：

```
#include <iostream>
#include <mutex>
#include <thread>

int v = 1;

void critical_section(int change_v) {
    static std::mutex mtx;
    std::lock_guard<std::mutex> lock(mtx);

    // 执行竞争操作
    v = change_v;

    // 离开此作用域后 mtx 会被释放
}

int main() {
    std::thread t1(critical_section, 2), t2(critical_section, 3);
    t1.join();
    t2.join();

    std::cout << v << std::endl;
    return 0;
}
```

由于 C++ 保证了所有栈对象在生命周期结束时会被销毁，所以这样的代码也是异常安全的。 无论 `critical_section()` 正常返回、还是在中途抛出异常，都会引发栈回溯，也就自动调用了 `unlock()`。

> 没有捕获抛出的异常（此时由实现定义是否进行栈回溯）。

而 `std::unique_lock` 则是相对于 `std::lock_guard` 出现的，`std::unique_lock` 更加灵活， `std::unique_lock` 的对象会以独占所有权（没有其他的 `unique_lock` 对象同时拥有某个 `mutex` 对象的所有权） 的方式管理 `mutex` 对象上的上锁和解锁的操作。所以在并发编程中，推荐使用 `std::unique_lock`。

`std::lock_guard` 不能显式的调用 `lock` 和 `unlock`， 而 `std::unique_lock` 可以在声明后的任意位置调用， 可以缩小锁的作用范围，提供更高的并发度。

如果你用到了条件变量 `std::condition_variable::wait` 则必须使用 `std::unique_lock` 作为参数。

例如：

```
#include <iostream>
#include <mutex>
#include <thread>

int v = 1;

void critical_section(int change_v) {
    static std::mutex mtx;
    std::unique_lock<std::mutex> lock(mtx);
    // 执行竞争操作
    v = change_v;
    std::cout << v << std::endl;
    // 将锁进行释放
    lock.unlock();

    // 在此期间，任何人都可以抢夺 v 的持有权

    // 开始另一组竞争操作，再次加锁
    lock.lock();
    v += 1;
    std::cout << v << std::endl;
}

int main() {
    std::thread t1(critical_section, 2), t2(critical_section, 3);
    t1.join();
    t2.join();
    return 0;
}
```



## 7.3 期物

期物（Future）表现为 `std::future`，它提供了一个访问异步操作结果的途径，这句话很不好理解。 为了理解这个特性，我们需要先理解一下在 C++11 之前的多线程行为。

试想，如果我们的主线程 A 希望新开辟一个线程 B 去执行某个我们预期的任务，并返回我一个结果。 而这时候，线程 A 可能正在忙其他的事情，无暇顾及 B 的结果， 所以我们会很自然的希望能够在某个特定的时间获得线程 B 的结果。

在 C++11 的 `std::future` 被引入之前，通常的做法是： 创建一个线程 A，在线程 A 里启动任务 B，当准备完毕后发送一个事件，并将结果保存在全局变量中。 而主函数线程 A 里正在做其他的事情，当需要结果的时候，调用一个线程等待函数来获得执行的结果。

而 C++11 提供的 `std::future` 简化了这个流程，可以用来获取异步任务的结果。 自然地，我们很容易能够想象到把它作为一种简单的线程同步手段，即屏障（barrier）。

为了看一个例子，我们这里额外使用 `std::packaged_task`，它可以用来封装任何可以调用的目标，从而用于实现异步的调用。 举例来说：

```
#include <iostream>
#include <future>
#include <thread>

int main() {
    // 将一个返回值为7的 lambda 表达式封装到 task 中
    // std::packaged_task 的模板参数为要封装函数的类型
    std::packaged_task<int()> task([](){return 7;});
    // 获得 task 的期物
    std::future<int> result = task.get_future(); // 在一个线程中执行 task
    std::thread(std::move(task)).detach();
    std::cout << "waiting...";
    result.wait(); // 在此设置屏障，阻塞到期物的完成
    // 输出执行结果
    std::cout << "done!" << std:: endl << "future result is "
              << result.get() << std::endl;
    return 0;
}
```

在封装好要调用的目标后，可以使用 `get_future()` 来获得一个 `std::future` 对象，以便之后实施线程同步。



## 7.4 条件变量

条件变量 `std::condition_variable` 是为了解决死锁而生，当互斥操作不够用而引入的。 比如，线程可能需要等待某个条件为真才能继续执行， 而一个忙等待循环中可能会导致所有其他线程都无法进入临界区使得条件为真时，就会发生死锁。 所以，`condition_variable` 对象被创建出现主要就是用于唤醒等待线程从而避免死锁。 `std::condition_variable`的 `notify_one()` 用于唤醒一个线程； `notify_all()` 则是通知所有线程。下面是一个生产者和消费者模型的例子：

```
#include <queue>
#include <chrono>
#include <mutex>
#include <thread>
#include <iostream>
#include <condition_variable>


int main() {
    std::queue<int> produced_nums;
    std::mutex mtx;
    std::condition_variable cv;
    bool notified = false;  // 通知信号

    // 生产者
    auto producer = [&]() {
        for (int i = 0; ; i++) {
            std::this_thread::sleep_for(std::chrono::milliseconds(900));
            std::unique_lock<std::mutex> lock(mtx);
            std::cout << "producing " << i << std::endl;
            produced_nums.push(i);
            notified = true;
            cv.notify_all(); // 此处也可以使用 notify_one
        }
    };
    // 消费者
    auto consumer = [&]() {
        while (true) {
            std::unique_lock<std::mutex> lock(mtx);
            while (!notified) {  // 避免虚假唤醒
                cv.wait(lock);
            }
            // 短暂取消锁，使得生产者有机会在消费者消费空前继续生产
            lock.unlock();
            // 消费者慢于生产者
            std::this_thread::sleep_for(std::chrono::milliseconds(1000));
            lock.lock();
            while (!produced_nums.empty()) {
                std::cout << "consuming " << produced_nums.front() << std::endl;
                produced_nums.pop();
            }
            notified = false;
        }
    };

    // 分别在不同的线程中运行
    std::thread p(producer);
    std::thread cs[2];
    for (int i = 0; i < 2; ++i) {
        cs[i] = std::thread(consumer);
    }
    p.join();
    for (int i = 0; i < 2; ++i) {
        cs[i].join();
    }
    return 0;
}
```

值得一提的是，在生产者中我们虽然可以使用 `notify_one()`，但实际上并不建议在此处使用， 因为在多消费者的情况下，我们的消费者实现中简单放弃了锁的持有，这使得可能让其他消费者 争夺此锁，从而更好的利用多个消费者之间的并发。话虽如此，但实际上因为 `std::mutex` 的排他性， 我们根本无法期待多个消费者能真正意义上的并行消费队列的中生产的内容，我们仍需要粒度更细的手段。



## 7.5 原子操作与内存模型

细心的读者可能会对前一小节中生产者消费者模型的例子可能存在编译器优化导致程序出错的情况产生疑惑。 例如，编译器可能对变量 `notified` 存在优化，例如将其作为一个寄存器的值， 从而导致消费者线程永远无法观察到此值的变化。这是一个好问题，为了解释清楚这个问题，我们需要进一步讨论 从 C++ 11 起引入的内存模型这一概念。我们首先来看一个问题，下面这段代码输出结果是多少？

```
#include <thread>
#include <iostream>

int main() {
    int a = 0;
    volatile int flag = 0;

    std::thread t1([&]() {
        while (flag != 1);

        int b = a;
        std::cout << "b = " << b << std::endl;
    });

    std::thread t2([&]() {
        a = 5;
        flag = 1;
    });

    t1.join();
    t2.join();
    return 0;
}
```

从直观上看，`t2` 中 `a = 5;` 这一条语句似乎总在 `flag = 1;` 之前得到执行，而 `t1` 中 `while (flag != 1)` 似乎保证了 `std::cout << "b = " << b << std::endl;` 不会再标记被改变前执行。从逻辑上看，似乎 `b` 的值应该等于 5。 但实际情况远比此复杂得多，或者说这段代码本身属于未定义的行为，因为对于 `a` 和 `flag` 而言，他们在两个并行的线程中被读写， 出现了竞争。除此之外，即便我们忽略竞争读写，仍然可能受 CPU 的乱序执行，编译器对指令的重排的影响， 导致 `a = 5` 发生在 `flag = 1` 之后。从而 `b` 可能输出 0。



### 原子操作

`std::mutex` 可以解决上面出现的并发读写的问题，但互斥锁是操作系统级的功能， 这是因为一个互斥锁的实现通常包含两条基本原理：

1. 提供线程间自动的状态转换，即『锁住』这个状态
2. 保障在互斥锁操作期间，所操作变量的内存与临界区外进行隔离

这是一组非常强的同步条件，换句话说当最终编译为 CPU 指令时会表现为非常多的指令（我们之后再来看如何实现一个简单的互斥锁）。 这对于一个仅需原子级操作（没有中间态）的变量，似乎太苛刻了。

关于同步条件的研究有着非常久远的历史，我们在这里不进行赘述。读者应该明白，现代 CPU 体系结构提供了 CPU 指令级的原子操作， 因此在多线程下共享变量的读写这一问题上， C++11 中还引入了 `std::atomic` 模板，使得我们能实例化原子类型，并将一个原子写操作从一组指令，最小化到单个 CPU 指令。例如：

```
std::atomic<int> counter;
```

并为整数或浮点数的原子类型提供了基本的数值成员函数，举例来说， 包括 `fetch_add`, `fetch_sub` 等，同时通过重载方便的提供了对应的 `+`，`-` 版本。 比如下面的例子：

```
#include <atomic>
#include <thread>
#include <iostream>

std::atomic<int> count = {0};

int main() {
    std::thread t1([](){
        count.fetch_add(1);
    });
    std::thread t2([](){
        count++;        // 等价于 fetch_add
        count += 1;     // 等价于 fetch_add
    });
    t1.join();
    t2.join();
    std::cout << count << std::endl;
    return 0;
}
```

当然，并非所有的类型都能提供原子操作，这是因为原子操作的可行性取决于具体的 CPU 架构，以及所实例化的类型结构是否能够满足该 CPU 架构对内存对齐 条件的要求，因而我们总是可以通过 `std::atomic<T>::is_lock_free` 来检查该原子类型是否需支持原子操作，例如：

```
#include <atomic>
#include <iostream>

struct A {
    float x;
    int y;
    long long z;
};

int main() {
    std::atomic<A> a;
    std::cout << std::boolalpha << a.is_lock_free() << std::endl;
    return 0;
}
```



### 一致性模型

并行执行的多个线程，从某种宏观层面上讨论，可以粗略的视为一种分布式系统。 在分布式系统中，任何通信乃至本地操作都需要消耗一定时间，甚至出现不可靠的通信。

如果我们强行将一个变量 `v` 在多个线程之间的操作设为原子操作，即任何一个线程在操作完 `v` 后， 其他线程均能**同步**感知到 `v` 的变化，则对于变量 `v` 而言，表现为顺序执行的程序，它并没有由于引入多线程 而得到任何效率上的收益。对此有什么办法能够适当的加速呢？答案便是削弱原子操作的在进程间的同步条件。

从原理上看，每个线程可以对应为一个集群节点，而线程间的通信也几乎等价于集群节点间的通信。 削弱进程间的同步条件，通常我们会考虑四种不同的一致性模型：

1. 线性一致性：又称强一致性或原子一致性。它要求任何一次读操作都能读到某个数据的最近一次写的数据，并且所有线程的操作顺序与全局时钟下的顺序是一致的。

   ```
           x.store(1)      x.load()
   T1 ---------+----------------+------>
   
   
   T2 -------------------+------------->
                   x.store(2)
   ```

   在这种情况下线程 `T1`, `T2` 对 `x` 的两次写操作是原子的，且 `x.store(1)` 是严格的发生在 `x.store(2)` 之前，`x.store(2)` 严格的发生在 `x.load()` 之前。 值得一提的是，线性一致性对全局时钟的要求是难以实现的，这也是人们不断研究比这个一致性更弱条件下其他一致性的算法的原因。

2. 顺序一致性：同样要求任何一次读操作都能读到数据最近一次写入的数据，但未要求与全局时钟的顺序一致。

   ```
           x.store(1)  x.store(3)   x.load()
   T1 ---------+-----------+----------+----->
   
   
   T2 ---------------+---------------------->
                 x.store(2)
   
   或者
   
           x.store(1)  x.store(3)   x.load()
   T1 ---------+-----------+----------+----->
   
   
   T2 ------+------------------------------->
         x.store(2)
   ```

   在顺序一致性的要求下，`x.load()` 必须读到最近一次写入的数据，因此 `x.store(2)` 与 `x.store(1)` 并无任何先后保障，即 只要 `T2` 的 `x.store(2)` 发生在 `x.store(3)` 之前即可。

3. 因果一致性：它的要求进一步降低，只需要有因果关系的操作顺序得到保障，而非因果关系的操作顺序则不做要求。

   ```
         a = 1      b = 2
   T1 ----+-----------+---------------------------->
   
   
   T2 ------+--------------------+--------+-------->
         x.store(3)         c = a + b    y.load()
   
   或者
   
         a = 1      b = 2
   T1 ----+-----------+---------------------------->
   
   
   T2 ------+--------------------+--------+-------->
         x.store(3)          y.load()   c = a + b
   
   亦或者
   
        b = 2       a = 1
   T1 ----+-----------+---------------------------->
   
   
   T2 ------+--------------------+--------+-------->
         y.load()            c = a + b  x.store(3)
   ```

   上面给出的三种例子都是属于因果一致的，因为整个过程中，只有 `c` 对 `a` 和 `b` 产生依赖，而 `x` 和 `y` 在此例子中表现为没有关系（但实际情况中我们需要更详细的信息才能确定 `x` 与 `y` 确实无关）

4. 最终一致性：是最弱的一致性要求，它只保障某个操作在未来的某个时间节点上会被观察到，但并未要求被观察到的时间。因此我们甚至可以对此条件稍作加强，例如规定某个操作被观察到的时间总是有界的。当然这已经不在我们的讨论范围之内了。

   ```
       x.store(3)  x.store(4)
   T1 ----+-----------+-------------------------------------------->
   
   
   T2 ---------+------------+--------------------+--------+-------->
            x.read      x.read()           x.read()   x.read()
   ```

   在上面的情况中，如果我们假设 x 的初始值为 0，则 `T2` 中四次 `x.read()` 结果可能但不限于以下情况：

   ```
   3 4 4 4 // x 的写操作被很快观察到
   0 3 3 4 // x 的写操作被观察到的时间存在一定延迟
   0 0 0 4 // 最后一次读操作读到了 x 的最终值，但此前的变化并未观察到
   0 0 0 0 // 在当前时间段内 x 的写操作均未被观察到，
           // 但未来某个时间点上一定能观察到 x 为 4 的情况
   ```



### 内存顺序

为了追求极致的性能，实现各种强度要求的一致性，C++11 为原子操作定义了六种不同的内存顺序 `std::memory_order` 的选项，表达了四种多线程间的同步模型：

1. 宽松模型：在此模型下，单个线程内的原子操作都是顺序执行的，不允许指令重排，但不同线程间原子操作的顺序是任意的。类型通过 `std::memory_order_relaxed` 指定。我们来看一个例子：

   ```
   std::atomic<int> counter = {0};
   std::vector<std::thread> vt;
   for (int i = 0; i < 100; ++i) {
       vt.emplace_back([&](){
           counter.fetch_add(1, std::memory_order_relaxed);
       });
   }
   
   for (auto& t : vt) {
       t.join();
   }
   std::cout << "current counter:" << counter << std::endl;
   ```

2. 释放/消费模型：在此模型中，我们开始限制进程间的操作顺序，如果某个线程需要修改某个值，但另一个线程会对该值的某次操作产生依赖，即后者依赖前者。具体而言，线程 A 完成了三次对 `x` 的写操作，线程 `B` 仅依赖其中第三次 `x` 的写操作，与 `x` 的前两次写行为无关，则当 `A` 主动 `x.release()` 时候（即使用 `std::memory_order_release`），选项 `std::memory_order_consume` 能够确保 `B` 在调用 `x.load()` 时候观察到 `A` 中第三次对 `x` 的写操作。我们来看一个例子：

   ```
   // 初始化为 nullptr 防止 consumer 线程从野指针进行读取
   std::atomic<int*> ptr(nullptr);
   int v;
   std::thread producer([&]() {
       int* p = new int(42);
       v = 1024;
       ptr.store(p, std::memory_order_release);
   });
   std::thread consumer([&]() {
       int* p;
       while(!(p = ptr.load(std::memory_order_consume)));
   
       std::cout << "p: " << *p << std::endl;
       std::cout << "v: " << v << std::endl;
   });
   producer.join();
   consumer.join();
   ```

3. 释放/获取模型：在此模型下，我们可以进一步加紧对不同线程间原子操作的顺序的限制，在释放 `std::memory_order_release` 和获取 `std::memory_order_acquire` 之间规定时序，即发生在释放（release）操作之前的**所有**写操作，对其他线程的任何获取（acquire）操作都是可见的，亦即发生顺序（happens-before）。

   可以看到，`std::memory_order_release` 确保了它之前的写操作不会发生在释放操作之后，是一个向后的屏障（backward），而 `std::memory_order_acquire` 确保了它之前的写行为不会发生在该获取操作之后，是一个向前的屏障（forward）。对于选项 `std::memory_order_acq_rel` 而言，则结合了这两者的特点，唯一确定了一个内存屏障，使得当前线程对内存的读写不会被重排并越过此操作的前后：

   我们来看一个例子：

   ```
   std::vector<int> v;
   std::atomic<int> flag = {0};
   std::thread release([&]() {
       v.push_back(42);
       flag.store(1, std::memory_order_release);
   });
   std::thread acqrel([&]() {
       int expected = 1; // must before compare_exchange_strong
       while(!flag.compare_exchange_strong(expected, 2, std::memory_order_acq_rel))
           expected = 1; // must after compare_exchange_strong
       // flag has changed to 2
   });
   std::thread acquire([&]() {
       while(flag.load(std::memory_order_acquire) < 2);
   
       std::cout << v.at(0) << std::endl; // must be 42
   });
   release.join();
   acqrel.join();
   acquire.join();
   ```

   在此例中我们使用了 `compare_exchange_strong` 比较交换原语（Compare-and-swap primitive），它有一个更弱的版本，即 `compare_exchange_weak`，它允许即便交换成功，也仍然返回 `false` 失败。其原因是因为在某些平台上虚假故障导致的，具体而言，当 CPU 进行上下文切换时，另一线程加载同一地址产生的不一致。除此之外，`compare_exchange_strong` 的性能可能稍差于 `compare_exchange_weak`，但大部分情况下，鉴于其使用的复杂度而言，`compare_exchange_weak` 应该被有限考虑。

4. 顺序一致模型：在此模型下，原子操作满足顺序一致性，进而可能对性能产生损耗。可显式的通过 `std::memory_order_seq_cst` 进行指定。最后来看一个例子：

   ```
   std::atomic<int> counter = {0};
   std::vector<std::thread> vt;
   for (int i = 0; i < 100; ++i) {
       vt.emplace_back([&](){
           counter.fetch_add(1, std::memory_order_seq_cst);
       });
   }
   
   for (auto& t : vt) {
       t.join();
   }
   std::cout << "current counter:" << counter << std::endl;
   ```

   这个例子与第一个宽松模型的例子本质上没有区别，仅仅只是将原子操作的内存顺序修改为了 `memory_order_seq_cst`，有兴趣的读者可以自行编写程序测量这两种不同内存顺序导致的性能差异。



## 总结

C++11 语言层提供了并发编程的相关支持，本节简单的介绍了 `std::thread`, `std::mutex`, `std::future` 这些并发编程中不可回避的重要工具。 除此之外，我们还介绍了 C++11 最重要的几个特性之一的『内存模型』， 它们为 C++ 在标准化高性能计算中提供了重要的基础。